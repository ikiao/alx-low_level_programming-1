#include "main.h"

/**
  * add - Adds two values
  * @s: The first number to be computed
  * @t: The second number to be computed
  *
  * Return: Integer value
  */
int add(int s, int t)
{
	return (s + t);
}
